#!/usr/bin/env bash

# 自動切換到 line 專案配置
echo "🔄 切換到 line-document-processor 配置..."
gcloud config configurations activate line-document-processor

# 顯示當前配置狀態
echo "📊 當前配置:"
gcloud config list --format="table(account,project,region,zone)"
